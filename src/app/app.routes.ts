import { Routes } from '@angular/router';
import { AuthComponent } from './pages/auth/auth.component'; // route de la page d'accueil connexion

export const routes: Routes = [

  { path: '', component: AuthComponent }

];